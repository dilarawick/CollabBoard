// Seed file: dummy tasks for local/dev setup.
// Note: boardId/columnId values reference seeded boards/columns.
// Usage example:
//   const Task = require('../models/Task');
//   const { tasks } = require('./index');
//   await Task.insertMany(tasks);
