// Seed file: dummy activities for local/dev setup.
// Note: boardId/taskId/userId values reference seeded data.
// Usage example:
//   const Activity = require('../models/Activity');
//   const { activities } = require('./index');
//   await Activity.insertMany(activities);
